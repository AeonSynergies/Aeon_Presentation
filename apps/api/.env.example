NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/aeon
JWT_ACCESS_SECRET=replace-with-a-long-random-string
WEB_ORIGIN=http://localhost:3000

# Optional — AI-assisted deck drafting (Phase 3a). Leave unset to run without it;
# ai.draftDeck then returns a clear "not configured" error instead of drafting.
# ANTHROPIC_API_KEY=
